# =============================================================================
# EdgeRouter (PiRouter Pro) — Environment Configuration
# Copy to .env and fill in your values
# =============================================================================

# Flask environment
FLASK_ENV=production

# Timezone
TZ=UTC

# Database path (inside container, matches app.py default)
# DB_PATH is hardcoded to /var/lib/pirouter/traffic.db in app.py
